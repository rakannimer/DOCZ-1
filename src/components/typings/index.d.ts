import * as React from "react";

export declare namespace ReactProps {
  interface Props extends React.Attributes {
    /**
     * React Node Children to pass through the component
     */
    children?: React.ReactNode;
  }
}
